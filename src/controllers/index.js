const authController = require('./authController');
const userController = require('./userController');
const aiSettingsController = require('./aiSettingsController');

module.exports = {
    authController,
    userController,
    aiSettingsController
};
